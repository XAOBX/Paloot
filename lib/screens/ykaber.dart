import 'package:flutter/material.dart';

class Ykaber extends StatelessWidget {
  const Ykaber({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
